<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mystore.crafthouse">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CraftHouse">
        <activity android:name=".mycart"></activity>
        <activity android:name=".AddProductActivity" />
        <activity android:name=".Seller" />
        <activity android:name=".Registration_Seller" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".Payment" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".best_for_you_row_item" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".Frame2" />
        <!--
<activity
            android:name=".navigation"
            android:label="@string/title_activity_navigation"
            android:theme="@style/Theme.CraftHouse.NoActionBar" />
        -->
        <activity android:name=".Reset_Password" />
        <activity android:name=".Registration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>